import React from 'react';

const UserListPage = () => {

    return (
        <div className="front-page">
            <p>This is the user listing page</p>
        </div>
    );
};

export default UserListPage;
